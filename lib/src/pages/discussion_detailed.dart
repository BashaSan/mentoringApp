import 'package:flutter/material.dart';

import '../services/discussion_service.dart';

class MyDiscussion extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        body: ChatPage(),
      );
}
